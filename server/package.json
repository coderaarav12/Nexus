{
  "name": "nexus-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nexus Server - central storage, scheduler, registry and API for Project Nexus",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
